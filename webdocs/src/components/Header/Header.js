import { StyledHeader } from "../../style/layout/StyledHeader";

function Header() {
    return (
        <StyledHeader>
            <p>Header</p>
        </StyledHeader>
    );
}

export default Header;
